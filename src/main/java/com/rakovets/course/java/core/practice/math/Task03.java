package com.rakovets.course.java.core.practice.math;

/**
 * Разработать программу для векторного графического редактора.
 *
 * @author Dmitry Rakovets
 */
@SuppressWarnings("unused")
class Task03 {
    /**
     * The entry point of the task.
     *
     * @param args entry arguments
     */
    public static void main(String[] args) {
        //FIXME
        // Ниже приведены значения присваиваемые переменным. Их можно изменять для проверки различных вариантов входных
        // аргументов. Типы данных изменять нельзя.
        float coordinateValue = 3.2f;

        long roundedCoordinateValue = roundCoordinateValue(coordinateValue);

        System.out.printf("Result: %b", roundedCoordinateValue);
    }

    /**
     * Округляет вещественное значение координаты для точки до ближайшего целого значения.
     *
     * @param coordinateValue - вещественное значение координаты точки
     * @return округленное значение координаты для точки
     */
    public static long roundCoordinateValue(float coordinateValue) {
        //TODO
        // Код, решающий задачу пишем ниже, при этом используя переменные объявленные выше (их можно изменять).
        // Для проверки решения необходимо запустить @Test для данного class (в директории test).
        return 0;
    }
}
